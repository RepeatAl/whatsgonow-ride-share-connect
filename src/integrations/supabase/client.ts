
/**
 * This file serves as a compatibility layer for existing imports.
 * It re-exports the supabase client from the standard location.
 */
import { supabase } from "@/lib/supabaseClient";

export { supabase };
