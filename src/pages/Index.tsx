
import React from "react";
import Landing from "./Landing";

// This component simply renders the Landing page
// It serves as the default index page for the root route
const Index = () => {
  return <Landing />;
};

export default Index;
